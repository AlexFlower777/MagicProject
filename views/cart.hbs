<body>
 <div class="Cart-Container">
   <div class="Header">
    <h1 class="Heading">Корзина</h1>
    <h3 class="Action">Удалить всё</h3>
    </div>
    {{#each card}}
    <div class="Cart-Items">
      <div class="image-box">
    </div>
    <div class="about">
      <h1 class="title">{{title}}</h1>
      <h2 class="condition">{{condition}}</h2>
      <h2 class="city">{{city}}</h2>
      <h2 class="seller">{{user}}</h2>
    </div>
    <div class="prices">
      <div class="amount">{{price}}</div>
      <div class="order"><u>Order</u></div>
      <div class="remove"><u>Remove</u></div>
    </div>
    {{/each}}
  </div>
 </div>
</body>
